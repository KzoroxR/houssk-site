# Optional for local/VPS builds of the public site.
# Required only if you want to connect the project to TinaCloud editing.
NEXT_PUBLIC_TINA_CLIENT_ID=
TINA_TOKEN=

# Optional branch override. Defaults to "main" when omitted.
NEXT_PUBLIC_TINA_BRANCH=main
